version: '3.8'

services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    image: cloudsense-backend:latest
    container_name: cloudsense-backend
    restart: unless-stopped
    ports:
      - "8082:8000"
    volumes:
      # Persistent SQLite database storage
      - cloudsense-db-data:/app/data
    environment:
      - QWEN_API_KEY=${QWEN_API_KEY}
      - QWEN_MODEL=${QWEN_MODEL:-qwen-plus}
      - QWEN_BASE_URL=${QWEN_BASE_URL:-https://dashscope-intl.aliyuncs.com/compatible-mode/v1}
      - ALIBABA_ACCESS_KEY_ID=${ALIBABA_ACCESS_KEY_ID}
      - ALIBABA_ACCESS_KEY_SECRET=${ALIBABA_ACCESS_KEY_SECRET}
      - ALIBABA_REGION=${ALIBABA_REGION:-us-east-1}
      - GITHUB_TOKEN=${GITHUB_TOKEN}
      - GITHUB_REPO_OWNER=${GITHUB_REPO_OWNER}
      - GITHUB_REPO_NAME=${GITHUB_REPO_NAME}
      - APP_ENV=production
      - APP_HOST=0.0.0.0
      - APP_PORT=8000
      - FRONTEND_URL=${FRONTEND_URL}
      - DATABASE_URL=sqlite:////app/data/cloudsense.db

volumes:
  cloudsense-db-data:
